// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/plexusone/omniagent/team/ent/allowlistentry"
	"github.com/plexusone/omniagent/team/ent/user"
)

// AllowlistEntry is the model entity for the AllowlistEntry schema.
type AllowlistEntry struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Email holds the value of the "email" field.
	Email string `json:"email,omitempty"`
	// AddedBy holds the value of the "added_by" field.
	AddedBy uuid.UUID `json:"added_by,omitempty"`
	// Note holds the value of the "note" field.
	Note string `json:"note,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AllowlistEntryQuery when eager-loading is set.
	Edges        AllowlistEntryEdges `json:"edges"`
	selectValues sql.SelectValues
}

// AllowlistEntryEdges holds the relations/edges for other nodes in the graph.
type AllowlistEntryEdges struct {
	// AddedByUser holds the value of the added_by_user edge.
	AddedByUser *User `json:"added_by_user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// AddedByUserOrErr returns the AddedByUser value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AllowlistEntryEdges) AddedByUserOrErr() (*User, error) {
	if e.AddedByUser != nil {
		return e.AddedByUser, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "added_by_user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AllowlistEntry) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case allowlistentry.FieldEmail, allowlistentry.FieldNote:
			values[i] = new(sql.NullString)
		case allowlistentry.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case allowlistentry.FieldID, allowlistentry.FieldAddedBy:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AllowlistEntry fields.
func (_m *AllowlistEntry) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case allowlistentry.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case allowlistentry.FieldEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field email", values[i])
			} else if value.Valid {
				_m.Email = value.String
			}
		case allowlistentry.FieldAddedBy:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field added_by", values[i])
			} else if value != nil {
				_m.AddedBy = *value
			}
		case allowlistentry.FieldNote:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field note", values[i])
			} else if value.Valid {
				_m.Note = value.String
			}
		case allowlistentry.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AllowlistEntry.
// This includes values selected through modifiers, order, etc.
func (_m *AllowlistEntry) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryAddedByUser queries the "added_by_user" edge of the AllowlistEntry entity.
func (_m *AllowlistEntry) QueryAddedByUser() *UserQuery {
	return NewAllowlistEntryClient(_m.config).QueryAddedByUser(_m)
}

// Update returns a builder for updating this AllowlistEntry.
// Note that you need to call AllowlistEntry.Unwrap() before calling this method if this AllowlistEntry
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AllowlistEntry) Update() *AllowlistEntryUpdateOne {
	return NewAllowlistEntryClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AllowlistEntry entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AllowlistEntry) Unwrap() *AllowlistEntry {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: AllowlistEntry is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AllowlistEntry) String() string {
	var builder strings.Builder
	builder.WriteString("AllowlistEntry(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("email=")
	builder.WriteString(_m.Email)
	builder.WriteString(", ")
	builder.WriteString("added_by=")
	builder.WriteString(fmt.Sprintf("%v", _m.AddedBy))
	builder.WriteString(", ")
	builder.WriteString("note=")
	builder.WriteString(_m.Note)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// AllowlistEntries is a parsable slice of AllowlistEntry.
type AllowlistEntries []*AllowlistEntry
